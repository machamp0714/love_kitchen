<div id="messages" data-room_id="<%= @room.id %>">
  <%= render @messages %>
</div>

<form id="messageForm"> 
  <textarea type="text" class="form-control" id="messageInput" placeholder='メッセージを入力してください'></textarea>
  <input type="submit" class="btn btn-default" value="送信">
</form>