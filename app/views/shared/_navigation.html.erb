<!-- Navigation -->
<nav class="navbar navbar-fixed-top navbar-expand-md navbar-dark bg-dark navbar-custom">
    <div class="container">
        <%= link_to 'LoveKitchen', root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-content">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbar-content">
            <ul class="navbar-nav mr-auto">
                <% if user_signed_in? %>
                    <li class="nav-item"> <%= link_to 'お気に入り', '#', class: 'nav-link' %> </li>
                    <li class="nav-item"> <%= link_to '投稿する', '#', class: 'nav-link' %> </li>
                    <li class="nav-item"> <%= link_to 'ログアウト', logout_path, method: :delete, class: 'nav-link' %> </li>
                <% else %>
                    <li class="nav-item"> <%= link_to 'About', '#', class: 'nav-link' %> </li>
                    <li class="nav-item"> <%= link_to '無料会員登録', signup_path, class: 'nav-link' %> </li>
                    <li class="nav-item"> <%= link_to 'ログイン', login_path, class: 'nav-link' %> </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>