<!-- Navigation -->
<nav class="navbar navbar-fixed-top navbar-expand-md navbar-dark bg-dark navbar-custom">
    <div class="container">
        <%= link_to 'LoveKitchen', root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-content">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbar-content">
            <ul class="navbar-nav ml-auto">
                <% if user_signed_in? %>
                    <li class="nav-item mr-sm-2 nav-search">
                        <%= render 'shared/search_form'%>
                    </li>
                    <li class="nav-item"> 
                        <%= link_to user_favorites_path(current_user), class: 'nav-link' do %>
                            <i class="fas fa-heart"></i> お気に入り
                        <% end %>
                    </li>
                    <li class="nav-item">
                        <%= link_to new_article_path, class: 'nav-link' do %>
                            <i class="fas fa-edit"></i> 投稿する
                        <% end %>
                    </li>
                    <li class="nav-item dropdown">
                        <% if already_read? %>
                            <a href="#" class="nav-link dropdown-toggle" id="notificationDropdown" rolw="button" data-toggle="dropdown"  aria-haspopup="true" aria-expanded="false">
                                0
                            </a>
                        <% else %>
                            <a href="#" class="nav-link dropdown-toggle" id="notificationDropdown" rolw="button" data-toggle="dropdown"  aria-haspopup="true" aria-expanded="false">
                                <%= Notification.count %>
                            </a>
                        <% end %>
                    </li>
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <%= image_tag current_user.avatar.to_s, size: '35x35'%>
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <%= link_to current_user, class: 'nav-link' do %>
                                <i class="fas fa-user-alt"></i> プロフィール
                            <% end %>
                            <div class="dropdown-divider"></div>
                            <%= link_to logout_path, method: :delete, class: 'nav-link' do %>
                                <i class="fas fa-sign-out-alt"></i> ログアウト
                            <% end %>
                        </div>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to about_path, class: 'nav-link' do %>
                            <i class="fas fa-utensils"></i> LoveKitchenとは
                        <% end %>
                    </li>
                    <li class="nav-item"> 
                        <%= link_to signup_path, class: 'nav-link' do %>
                            <i class="fas fa-rocket"></i> 無料会員登録
                        <% end %>
                    </li>
                    <li class="nav-item"> 
                        <%= link_to login_path, class: 'nav-link' do %>
                            <i class="fas fa-lock"></i> ログイン
                        <% end %>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>